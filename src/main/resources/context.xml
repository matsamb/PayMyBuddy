<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:aop="http://www.springframework.org/schema/aop"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/tx
        https://www.springframework.org/schema/tx/spring-tx.xsd
        http://www.springframework.org/schema/aop
        https://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- this is the service object that we want to make transactional -->
    <bean id="saveAuthorityService" class="com.paymybuddy.service.authority.AuthoritiesSaveAuthorityService"/>
    
    <bean id="findBalanceService" class="com.paymybuddy.service.balance.FindBalanceService"/>
    <bean id="saveBalanceService" class="com.paymybuddy.service.balance.SaveBalanceService"/>
    <bean id="updateBalanceService" class="com.paymybuddy.service.balance.UpdateBalanceService"/>
    
    <bean id="addConnectionService" class="com.paymybuddy.service.connection.AddConnectionService"/>
    <bean id="findByPayeeConnectionService" class="com.paymybuddy.service.connection.FindEconnectionByPayeeUsernameService"/>
    <bean id="findByPayerConnectionService" class="com.paymybuddy.service.connection.FindEconnectionByPayerUsernameService"/>
    
    <bean id="findPaymentByEconnectionIdService" class="com.paymybuddy.service.payment.FindPaymentByEconnectionIdService"/>
    <bean id="savePaymentService" class="com.paymybuddy.service.payment.SavePaymentService"/>

    <bean id="checkUsersService" class="com.paymybuddy.service.users.CheckUsersService"/>
    <bean id="saveUserService" class="com.paymybuddy.service.users.UsersSaveUserService"/>
    

    <!-- the transactional advice (what 'happens'; see the <aop:advisor/> bean below) -->
    <tx:advice id="txAdvice" transaction-manager="txManager">
        <!-- the transactional semantics... -->
        <tx:attributes>
            <!-- all methods starting with 'find' are read-only -->
            <tx:method name="find*" read-only="true"/>
		    <tx:method name="*" rollback-for="Throwable" />

        </tx:attributes>
    </tx:advice>
 
 <!-- ensure that the above transactional advice runs for any execution
        of an operation defined by the Services  -->
    <aop:config>
        <aop:pointcut id="serviceOperation" expression="execution(* com.paymybuddy.service..*Service.*(..))"/>
        <aop:advisor pointcut-ref="serviceOperation" advice-ref="txAdvice"/>
    </aop:config>

    <!-- don't forget the DataSource -->
    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!-- similarly, don't forget the TransactionManager -->
    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- other <bean/> definitions here -->

</beans> 